#include<stdio.h>
int main(){
    printf("Shubham kumar\n");
    int x,y,z;
    printf("Enter your first number x:");
    scanf("%d",&x);
    printf("Enter your first number y:");
    scanf("%d",&y);
    z=x;
    x=y;
    y=z;
    printf("After swaping two numbers x = %d and y = %d",x,y);
    return 0;
}
